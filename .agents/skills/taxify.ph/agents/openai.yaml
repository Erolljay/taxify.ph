interface:
  display_name: "Taxify.ph"
  short_description: "Repo-specific patterns and workflows for taxify.ph"
  default_prompt: "Use the taxify.ph repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true